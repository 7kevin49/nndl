[tool.poetry]
name = "nndl"
version = "0.1.0"
description = ""
authors = ["Kevin Perdomo"]
packages = [
    {include = "nndl"}
    ]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
numpy = "^2.2.3"
jax = {extras = ["cuda12"], version = "^0.5.1"}
structlog = "^25.1.0"
jupyter = "^1.1.1"
scikit-learn = "^1.6.1"
pandas = "^2.2.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
